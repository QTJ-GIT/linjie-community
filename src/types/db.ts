// Placeholder until `npx supabase gen types typescript --project-id <id>` is run in Phase 2.
// For now the app uses hand-written types from @/types/domain.
export type Database = Record<string, unknown>;
